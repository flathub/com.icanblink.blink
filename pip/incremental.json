{
    "name": "python2-incremental",
    "buildsystem": "simple",
    "build-commands": [
        "pip2 install --upgrade --no-index --find-links=\"file://${PWD/sources}\" --prefix=${FLATPAK_DEST} incremental --no-build-isolation"
    ],
    "sources": [
        {
            "type": "dir",
            "path": "sources"
        }
    ]
}
