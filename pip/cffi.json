{
    "name": "python2-cffi",
    "buildsystem": "simple",
    "build-commands": [
        "pip2 install --upgrade --no-index --find-links=\"file://${PWD/sources}\" --prefix=${FLATPAK_DEST} cffi --no-build-isolation"
    ],
    "sources": [
        {
            "type": "dir",
            "path": "sources"
        }
    ]
}
