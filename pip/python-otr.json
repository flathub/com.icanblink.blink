{
    "name": "python2-python-otr",
    "buildsystem": "simple",
    "build-commands": [
        "pip2 freeze --local",
        "pip2 install --upgrade --no-index --find-links=\"file://${PWD/sources}\" --prefix=${FLATPAK_DEST} python-otr --no-build-isolation"
    ],
    "sources": [
        {
            "type": "dir",
            "path": "sources"
        }
    ]
}
