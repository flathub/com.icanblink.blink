{
    "name": "python2-Twisted",
    "buildsystem": "simple",
    "build-commands": [
        "pip2 install --upgrade --no-index --find-links=\"file://${PWD/sources}\" --prefix=${FLATPAK_DEST} Twisted --no-build-isolation"
    ],
    "sources": [
        {
            "type": "dir",
            "path": "sources"
        }
    ]
}
